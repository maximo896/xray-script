{
  "log": {"loglevel": "warning"},
  "inbounds": [
    {
      "tag": "mixed-in",
      "port": 10828,
      "listen": "0.0.0.0",
      "protocol": "mixed",
      "settings": {
        "auth": "noauth",
        "udp": true,
        "userLevel": 0
      }
    }
  ],
  "outbounds": [
    {
      "protocol": "socks",
      "settings": {
        "servers": [
          {
            "address": "proxy.wandouip.com",
            "port": 2080,
            "users": []
          }
        ]
      },
      "tag": "proxy"
    },
    {"protocol": "freedom", "tag": "direct"},
    {"protocol": "blackhole", "tag": "blocked"}
  ],
  "routing": {
    "domainStrategy": "AsIs",
    "rules": [
      {"type": "field", "outboundTag": "proxy", "network": "tcp,udp"}
    ]
  }
}
